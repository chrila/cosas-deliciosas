<%= form_with(model: beer) do |form| %>
  <% if beer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beer.errors.count, "error") %> prohibited this beer from being saved:</h2>

      <ul>
        <% beer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :brewery_id %>
    <%= form.select :brewery_id, breweries_for_select, {}, class: "custom-select" %>
  </div>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="form-group">
    <%= form.label :beer_style_id %>
    <%= form.select :beer_style_id, beer_styles_for_select, {}, class: "custom-select" %>
  </div>

  <div class="form-group">
    <%= form.label :ibu, 'IBU' %>
    <%= form.number_field :ibu, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :abv, 'ABV [%]' %>
    <%= form.number_field :abv, step: 0.1, class: "form-control" %>
  </div>

  <div class="actions form-group">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
