<%= form_with(model: serving_format) do |form| %>
  <% if serving_format.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(serving_format.errors.count, "error") %> prohibited this serving_format from being saved:</h2>

      <ul>
        <% serving_format.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
